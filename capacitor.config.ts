import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ADSO-24B2-Facturacion-FrontEnd-main',
  webDir: 'www'
};

export default config;
